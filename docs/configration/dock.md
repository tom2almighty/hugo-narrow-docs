# Dock

Hugo Narrow implements common functionality through a bottom dock, including back to previous page, back to top, jumping to comments, search, and table of contents.

## Parameters

The dock can be configured for display and interaction modes, supporting three types: always visible, show on scroll up, and floating.

```yaml
params:
  dock: "float" # always/float/scroll
```
